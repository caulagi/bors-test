#!groovy

pipeline {
    agent any

    stages {
        stage('Hello deploy') {
            when {
                expression { true }
            }
            steps {
                echo 'ok'
            }
        }
    }
}
